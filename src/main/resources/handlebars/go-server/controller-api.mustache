package {{packageName}}

{{>partial_header}}

{{#operations}}// Register this API group with the central router in router.go.
func init() {
	swaggerRoutes = append(swaggerRoutes, swagger{{classname}}Routes)
}

var swagger{{classname}}Routes = []swaggerRoute{
{{#operation}}
	swaggerRoute{
		"{{operationId}}",
		"{{httpMethod}}",
		"{{{basePathWithoutHost}}}{{{path}}}",
		{{operationId}},
	},{{/operation}}
}
{{/operations}}
